function pow(a, n) {
  let result = a;

  for (let i = 1; i < n; i++) {
    result *= a;
  }

  return result;
}

let x = prompt("a?", '');
let n = prompt("n?", '');

if (n < 1) {
  alert(`Power ${n} is not supported, use a positive integer`);
} else {
  alert( pow(a, n) );
}
